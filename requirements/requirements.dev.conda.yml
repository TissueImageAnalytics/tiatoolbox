name: tiatoolbox-dev
channels:
  - conda
  - conda-forge
  - pytorch
  - defaults
dependencies:
  - cython
  - openslide
  - pip>=20.0.2
  - pixman>=0.39.0
  - python>=3.10, <=3.13
  - pip:
      - -r requirements_dev.txt

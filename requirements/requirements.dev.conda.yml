name: tiatoolbox-dev
channels:
  - conda
  - conda-forge
  - pytorch
  - defaults
dependencies:
  - cython
  - pip>=20.0.2
  - pixman>=0.39.0
  - python>=3.11, <=3.14
  - pip:
      - -r requirements_dev.txt
